before:
  hooks:
    - go mod tidy
builds:
  - goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
    hooks:
      pre: pkger -include /payloads -include /go.mod -o ./cmd
      post: rm ./cmd/pkger.go
checksum:
  name_template: 'checksums.txt'